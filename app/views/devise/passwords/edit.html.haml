= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { class: 'form-forgot-password-edit-heading', role: 'form', method: :put }) do |f|
  %h2.form-signin-heading Cambiar su contraseña
  = f.error_notification
  = f.input :reset_password_token, :as => :hidden
  = f.full_error :reset_password_token
  = f.password_field :password, class: 'form-control', placeholder: "Nueva constraseña", type: "password"
  = f.password_field :password_confirmation, class: 'form-control', placeholder: "Confirmar su nueva contraseña", type: "password"
  %button.btn.btn-lg.btn-primary.btn-block{type: "submit"} Cambiar mi contraseña
= render "devise/shared/links"
